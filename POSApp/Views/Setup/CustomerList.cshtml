@model IEnumerable<POSApp.Core.Models.Customer>

@{
    ViewBag.Title = "CustomerList";
}
<section role="main" class="content-body">
    @*<header class="page-header">
        <h2>Customers</h2>

        <div class="right-wrapper pull-right">
            <ol class="breadcrumbs">
                <li>
                    <a href="@Url.Action("Index","Home")">
                        <i class="fa fa-home"></i>
                    </a>
                </li>
                <li><span>Customers</span></li>
                <li><span>Setup</span></li>
            </ol>

            <a class="sidebar-right-toggle" data-open="sidebar-right"><i class="fa fa-chevron-left"></i></a>
        </div>
    </header>*@

    <!-- start: page -->
    <section class="panel">
        <header class="panel-heading">
            <div class="row">
                <div class="col-md-12">
                    <div class="" style="display: flex; justify-content: space-between;align-items: center">
                        <h3><b>Customers</b></h3>
                        <a href="@Url.Action("AddCustomer", "Setup")" class="modalShow">
                            <span class="fa fa-2x fa-plus-circle"></span>
                        </a>
                    </div>
                </div>
            </div>

            <h2 class="panel-title"></h2>
        </header>
        <div class="panel-body">
            <table class="table mb-none" style="width:100%" id="datatable-default">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Email</th>
                        <th>Mobile Number</th>
                        <th>Address</th>
                        <th>Action</th>

                    </tr>
                </thead>
                <tbody>
                    @{ int i = 1;}
                    @foreach (var customer in Model)
                    {
                        <tr>
                            <td>@(i++)</td>
                            <td>@customer.Name</td>
                            <td>@customer.Email</td>
                            <td>@customer.PhoneNumber</td>
                            <td>@customer.Address</td>
                            <td class="center">
                                @Html.ActionLink(" ", "UpdateCustomer", "Setup", new { id = @customer.Id,storeId=@customer.StoreId }, new { @class = "btn btn-default btn-sm fa fa-pencil modalShow" })
                                @Html.ActionLink(" ", "DeleteCustomer", "Setup", new { id = @customer.Id, storeId = @customer.StoreId }, new { @class = "btn btn-danger btn-sm fa fa-trash-o", @style = "color: #ffffff;", onclick = "return confirm('Are you sure?');" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </section>




</section>

<div id="ModalData"></div>
@section scripts
{
    <script>
        $('#datatable-default').dataTable();

        $('a.modalShow').on('click', function () {

            $.ajax({
                url: this.href,
                type: 'GET',
                cache: false,
                success: function (result) {
                    $('#ModalData').html(result).find('#ordine').modal('show');
                }
            });

            return false;
        });
    </script>
}


