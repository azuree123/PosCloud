@model IEnumerable<POSApp.Core.Models.PurchaseOrderDetail>

@{
    ViewBag.Title = "PurchaseOrdersDetailsList";
}
<section role="main" class="content-body">
    <header class="page-header">
        <h2>PurchaseOrdersDetails</h2>

        <div class="right-wrapper pull-right">
            <ol class="breadcrumbs">
                <li>
                    <a href="@Url.Action("Index","Home")">
                        <i class="fa fa-home"></i>
                    </a>
                </li>
                <li><span>PurchaseOrdersDetail</span></li>
                
            </ol>

            <a class="sidebar-right-toggle" data-open="sidebar-right"><i class="fa fa-chevron-left"></i></a>
        </div>
    </header>

    <!-- start: page -->
    <section class="panel">
        <header class="panel-heading">
            <div class="panel-actions">
                <a href="#" class="panel-action panel-action-toggle" data-panel-toggle></a>
            </div>

            <h2 class="panel-title"></h2>
        </header>
        <div class="panel-body">
            <table class="table table-bordered table-striped mb-none" style="width:100%" id="datatable-default">
                <thead>
                    <tr>
                        <th width="5%">ID</th>
                        <th width="15%">PurchaseOrder</th>
                        <th width="15%">Product</th>
                        <th width="15%">Quantity</th>
                        <th width="5%">Retail Price</th>
                        <th width="15%">Discount</th>
                        <th width="10%">Unit Price</th>
                        <th width="5%">Action</th>

                    </tr>
                </thead>
                <tbody>
                    @{ int i = 1;}
                    @foreach (var purchaseOrderDetail in Model)
                    {
                        <tr>
                            <td>@(i++)</td>
                            <td>@purchaseOrderDetail.PurchaseOrderId</td>
                            <td>@purchaseOrderDetail.ProductId</td>
                            <td>@purchaseOrderDetail.Quantity</td>
                            <td>@purchaseOrderDetail.RetailPrice</td>
                            <td>@purchaseOrderDetail.Discount</td>
                            <td>@purchaseOrderDetail.UnitPrice</td>
                            <td></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </section>




</section>
<div id="ModalData"></div>
@section scripts
{
    <script>
        $('#datatable-default').dataTable();

        $('a.modalShow').on('click', function () {

            $.ajax({
                url: this.href,
                type: 'GET',
                cache: false,
                success: function (result) {
                    $('#ModalData').html(result).find('#ordine').modal('show');
                }
            });

            return false;
        });
    </script>
}



